general {
    output_format = "i3bar"
    colors = true
    interval = 2

    color_good = "#718C00"
    color_degraded = "#EAB700"
    color_bad = "#D54E53"
}

order += "tztime local"
order += "wireless wlp2s0"
order += "battery 1"
order += "volume master"

wireless wlp2s0 {
    format_up = "⬆⬇ %essid"
    format_down = "⬆⬇ Non connecté"
    min_width = 115
}

battery 1 {
    format = "%status %percentage %remaining"
    integer_battery_capacity = true
    last_full_capacity = true
    low_threshold = 20
    threshold_type = percentage
    status_chr = "⚉"
    status_bat = "⚡"
    status_full = "🅵"
    min_width = 100
}

tztime local {
    format = "%A %d %B %H:%M"
    align = center
    min_width = 710
}

volume master {
    format = "🔉%volume"
    format_muted = "🔇Mute"
    device = "default"
    mixer = "Master"
    mixer_idx = 0
    min_width = 60
}
